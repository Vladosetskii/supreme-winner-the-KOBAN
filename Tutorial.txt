how to compile file - main.cpp
--------------------------------
3.g++ -c main.cpp
4.g++ main.o -o <launcher name>
5.<launcher name>


Open file startProg.exe if it does not open 
-------------------------------------------------
specify the path to the file startProg.exe in cmd
and click OK
